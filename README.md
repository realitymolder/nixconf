every config and folder can be a feature by itself and an experience!
So... every module that build it together will give it its full power
which means that there


every module is also able to be imported via flake-parts feature of self.X
which will allow you to call them right on the config file - or create an extention module for each host-config.
